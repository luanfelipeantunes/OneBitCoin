import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice: {
        width: "100%",
        height: "auto",
        alignItems: "center",
        marginBottom: 24,
        marginTop: 20,
    },
    currentPrice: {
        color: "#F20505",
        fontSize: 32,
        fontWeight: "bold",

    },
    textPrice: {
        color: "#FFFFFF",
        fontSize: 16,
        fontWeight: "bold",
    }
})

export default styles;